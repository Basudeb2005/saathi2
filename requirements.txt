# Agent runtime
# openai+silero is all you need to start; add the others only if you
# switch STT_PROVIDER/TTS_PROVIDER.
livekit-agents[openai,silero]~=1.0
# livekit-agents[deepgram,elevenlabs]
livekit-api~=0.8

# Wake word (runs on-device). openwakeword is the default and needs
# no account; pvporcupine is only needed for WAKE_ENGINE=porcupine.
openwakeword
numpy
# pvporcupine

# Restores stdlib `audioop`, removed in Python 3.13 (PEP 594).
audioop-lts; python_version >= "3.13"

# Music + config
requests
python-dotenv

# Tests
pytest
